# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Arquivos de ambiente (NUNCA envie arquivos .env com segredos!)
.env
.env*.local
.env.development.local
.env.test.local
.env.production.local

# Dependências do Node.js (backend e frontend)
node_modules/
backend/node_modules/  # Ajuste se o nome da pasta do backend for diferente
frontend/node_modules/  # Ajuste se o nome da pasta do frontend for diferente

# Arquivos de build do frontend React
frontend/build/
frontend/dist/ # Se usar Vite ou similar

# Arquivos de build do backend (se aplicável, ex: TypeScript)
backend/dist/

# Arquivos gerados pelo Sistema Operacional
.DS_Store
Thumbs.db

# Outros (opcional, dependendo do seu editor)
# .idea/
# .vscode/